#pragma once
#ifndef SELECTION_SORT_H
#define SELECTION_SORT_H

// Selection Sort prototype
void selection_sort(int* arr, int size);

// Optional helper function to swap two integers
void swap_ints(int& a, int& b);

#endif